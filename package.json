{
  "name": "video-downloader",
  "version": "1.1.4",
  "private": true,
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "version:set": "node scripts/set-version.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
